server:
  port: 8083
spring:
  application:
    name: asset-service #指定服务名称
  datasource:
    username: root
    password: sixinbuxi
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://zrylovestan.com:3306/microservices?useUnicode=true&characterEncoding=utf-8&allowMultiQueries=true&useSSL=false #可考虑加一个查询参数
#  datasource:
#    hikari:
#      driver-class-name: com.mysql.cj.jdbc.Driver
#
#      jdbc-url: jdbc:mysql://192.168.1.13:3306/microservices #可考虑加一个查询参数
#      username: root
#      password: alan
mybatis: #mybatis
  #  type-aliases-package: com.alan.microservices.domain
  configuration:
    map-underscore-to-camel-case: true
    default-fetch-size: 100
    default-statement-timeout: 3000
  mapper-locations: classpath:mybatis/mapper/*.xml


eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true #将本机ip注册到eureka server,而不是hostname

#management:
#  endpoints:
#    web:
#      exposure:
logging:
  level:
    com.alan.microservices.asset.controller.MosAuthController: debug
    com.alan.microservices.asset.service.MosAuthService: debug
#        include: '*'